@model IEnumerable<BusinessProject.Models.typeProvider>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameTypeProvider)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descriptionProvider)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameTypeProvider)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descriptionProvider)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProvider }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProvider }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProvider })
        </td>
    </tr>
}

</table>
